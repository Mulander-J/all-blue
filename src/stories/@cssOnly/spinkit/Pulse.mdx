import { Meta, Unstyled } from '@storybook/blocks';

<Meta />

<style>
{`
    .sk-pulse {
        --sk-size: 40px;
        --sk-color: #1ea7fd;
        width: var(--sk-size);
        height: var(--sk-size);
        background-color: var(--sk-color);
        border-radius: 100%;
        animation: sk-pulse 1.2s infinite cubic-bezier(0.455, 0.03, 0.515, 0.955);
    }

    @keyframes sk-pulse {
        0% {
            transform: scale(0);
        } 100% {
            transform: scale(1);
            opacity: 0;
        }
    }
`}
</style>

# Pulse

<Unstyled>
    <div className="p-20">
        <div className="sk-pulse"></div>
    </div>
</Unstyled>

---

```html
<div className="sk-pulse"></div>
```

```css
.sk-pulse {
    width: var(--sk-size);
    height: var(--sk-size);
    background-color: var(--sk-color);
    border-radius: 100%;
    animation: sk-pulse 1.2s infinite cubic-bezier(0.455, 0.03, 0.515, 0.955);
}

@keyframes sk-pulse {
    0% {
        transform: scale(0);
    } 100% {
        transform: scale(1);
        opacity: 0;
    }
}
```

> Reference:
> - [spinkit-demo](https://tobiasahlin.com/spinkit/)
> - [spinkit-official](https://github.com/tobiasahlin/SpinKit)