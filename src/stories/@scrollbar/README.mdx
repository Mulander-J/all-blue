import { Meta, Story } from "@storybook/blocks";
import * as GradientStory from './GradientBar/index.stories';

<Meta />

# Scrollbar

## Override CSS

覆盖原始滚动条样式

```
::-webkit-scrollbar    //滚动条整体部分
::-webkit-scrollbar-button   //滚动条两端的按钮
::-webkit-scrollbar-track   // 外层轨道
::-webkit-scrollbar-track-piece    //内层轨道，滚动条中间部分（除去）
::-webkit-scrollbar-thumb //滚动条里面可以拖动的那个
::-webkit-scrollbar-corner   //边角
::-webkit-resizer   ///定义右下角拖动块的样式
```

## Custom Element

隐藏原始滚动条

```css
&::-webkit-scrollbar{
    width: 0;
}
```
添加自定义元素，以及滚动监听
```js
const handleCroll = (event) => {
    let { scrollTop, scrollHeight, clientHeight } = event.target;
    percent = (100 * scrollTop) / (scrollHeight - clientHeight);
};
```

### e.g. Gardient Bar

<Story of={GradientStory.Preview} />